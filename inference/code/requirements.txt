# SageMaker Inference Dependencies
# These packages will be installed in the SageMaker container

# Deep Learning Framework
torch==2.0.1

# Data Processing - use flexible numpy version for compatibility
numpy>=1.23.0,<2.0.0
scikit-learn==1.3.0

# Note: boto3 is already available in SageMaker containers, no need to install
